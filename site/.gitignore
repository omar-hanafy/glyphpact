# Build output. The Pages workflow rebuilds it from source on every deploy.
dist/

# Astro's generated types and cache.
.astro/

# Dependencies. package-lock.json is committed so installs are reproducible.
node_modules/

# Browser-test artifacts. CI retains traces only when a run fails.
playwright-report/
test-results/
